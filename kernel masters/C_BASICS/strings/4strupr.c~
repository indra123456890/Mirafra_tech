#include<stdio.h>
void strupr(char upp[]);
int main()
{
char upp[50];
{
scanf("%s",upp);
strupr(upp);
printf("%s",upp);
return 0;
}
}

void strupr(char upp[])
{
int i;
for(i=0;upp[i]!='\0';i++)
{
if(upp[i]>=97&&upp[i]<=122)
{
upp[i]=upp[i]-32;
}
}
}



